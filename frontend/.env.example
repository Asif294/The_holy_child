# Base URL of the Django API. Leave as the relative default to use the Vite dev proxy.
VITE_API_BASE_URL=/api/v1

# Where the dev proxy forwards /api and /media
VITE_PROXY_TARGET=http://127.0.0.1:8000

# Dev server port
VITE_PORT=5173
